setup:
  addons:
  - plan: heroku-postgresql
    as: rust2prod
build:
  docker:
    release:
       dockerfile: Dockerfile
       target: builder
    web: Dockerfile
    worker: worker/Dockerfile
run:
  web: sqlx migrate run